[project]
name = "fitbit-music"
version = "0.1.0"
description = "Fitbit API data to near-real-time music parameters pipeline"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["fitbit_music"]
